# DEPRECATED (2026-07-13). Dependencies are now declared in pyproject.toml and
# frozen in uv.lock. Set up with:  uv sync
# Optional ML/GPU extras:  uv sync --extra pose|ml|gpu
# See rb3/docs/dev/python-tooling-uv.md
