# Core build tools
pcpp

# C/C++ parsing (permuter, analysis scripts)
tree-sitter==0.25.2
tree-sitter-cpp==0.23.4

# Orchestrator MCP server
mcp>=1.0.0
pydantic>=2.0.0
click>=8.1
python-dotenv>=1.0.0
requests>=2.28.0

# Ghidra integration
pyghidra>=3.0.0

# Unicorn CPU emulator (behavioral testing)
unicorn>=2.0.0

# GPU debugging (RenderDoc CLI wrapper)
rdc-cli>=0.4.0
