[
  {"symbol_like": "?IsPlaying@Voice@@QAA_NXZ", "unit_like": "%synth_xbox/Voice", "cert": "artifact:regalloc", "expect_pct": 98.0,
   "evidence": {"source_doc": "wave-15 lane C 2026-06-21 (diagnose + permuter @ w15-struct from d5110265)", "diagnosis": "Condition-register-field allocation floor. Source is faithful (the mState early-outs + GetVoice() null check + state.BuffersQueued==0 && state.SamplesPlayed==0 + params.unkc==0.0f tail). Residual is the CR-field the compiler picks: TGT idx40 cmplwi r3,0x0 (compares into CR0) + idx42 bne 0xb4, vs SRC cmplwi cr6,r3,0x0 + bne cr6,0xb8 — same compare, opposite CR field; plus one delete at idx41 and 17 diff_arg that are CR-field renumbering downstream. Permuter exhausted (0 improvement over 50 variants x 6 rounds, all survivors plateau 98.02; comparison_flip/bool_cast/constraint_solver all SEMANTIC_OK but no gain). Unicorn class cap_exhausted_orig (original itself not fully traceable by the behavioral cap). CR-field selection is not source-controllable — backend register allocation artifact."}},

  {"symbol_like": "?compute_apres@@YAXXZ", "unit_like": "%synth/filterdesign", "cert": "artifact:build_env", "expect_pct": 42.3,
   "blocked_by": "stale_is_stub_flag",
   "evidence": {"source_doc": "wave-15 lane C 2026-06-21 (diagnose @ w15-struct from d5110265)", "blocker": "decomp.db flags this row is_stub=1 (FALSE POSITIVE — the function IS implemented at 42.26%; the byte-size heuristic fires because the target's inlined body is ~6x larger than our base TU). certify_floor.py requires is_stub=0 for every class except native_divergence, so this floor CANNOT be certed until the orchestrator (single DB writer) clears the stale is_stub flag for ?compute_apres@@YAXXZ. The other two entries in this file resolve cleanly; this one is recorded so the orchestrator can fix is_stub then cert.", "diagnosis": "Inlining-heuristic / build-environment floor. The target inlines a far larger body than our instantiation emits: 42 deletes across two large clusters (idx 10-32 = 18D, idx 37-66 = 22D) where the target open-codes the apres-ski filter computation against a large labeled local buffer (idx35 TGT addi r9,r31,0x2000,lbl_83172BB0 — an ~8KB local addressed via a const-table base reg + lbl_83172BB0 sentinel, vs SRC addi r3,r1,0x60 small-frame compute), while our source calls out to / lays out the same math differently. Only 9/74 instructions equal (~12% raw; report normalized 42.26 = expect_pct). The divergence is which helpers the MSVC build inlined into compute_apres and the resulting large-buffer frame layout, not a behavioral or source-authorable difference — a compiler inlining/build-environment artifact (closest valid cert class; 'artifact:codegen' is not an accepted floor class in certify_floor.py). Frame-offset shifts (+8, +80, -32, -72, +8184) and 16 GPR regswap pairs are all downstream of the inlined-body size delta."}},

  {"symbol_like": "?XfmOnCircleEdge@@YA?AVTransform@@MM@Z", "unit_like": "%rndobj/Text", "cert": "artifact:regalloc", "expect_pct": 78.93,
   "evidence": {"source_doc": "wave-15 lane C 2026-06-21 (landed safe reorder 77.3->78.9 then certed floor @ w15-struct commit 6f513f03)", "diagnosis": "FPR volatile-register coloring + scheduling floor. Source is faithful (sign-of-circumference, Cosine/Cosine-via-Sine-identity angle, the y=cosA/sinA*-sign row, the x = y cross z cross-product, and xfm.v *= radius). Landed two behavior-preserving edits that recovered +1.6%: sign ternary->if/else (lowers to fsel matching target) and moving xfm.v.Set between the two independent xfm.m.y assignments. REJECTED the permuter statement_reorder that hoisted xfm.m.x.y = xfm.m.y.z*... above xfm.m.y.z = 0.0f*negSign — a use-before-def of m.y.z (Transform xfm is NOT value-initialized), a real correctness bug despite SEMANTIC_OK. Residual ~21% is dominated by volatile-FPR swaps f12<->f13 (6 of 15), f10<->f9 in the cross-product fmsubs chain and the final v*=radius scale, plus a (0x0,0x4) store-offset swap and 2 SWAPPED stack slots — all post-regalloc FPR coloring/scheduling the source cannot steer. The cross-products cannot be reordered (m.y.z data dependency). Unicorn class call_arg is a downstream artifact of the FPR coloring across the single Sine call (cosA held in callee-saved f27), not a real wrong-argument."}}
]
