# Known-bad entries for `python3 scripts/test_tools.py` — this repo's test lane.
#
# Baseline measured 2026-08-17 on this box at branch point 37614e655, one pytest
# process per collection root. The runner reports these and does not fail on
# them; anything NOT listed here that fails makes the runner exit non-zero.
#
# Format: one pytest id per line, `#` starts a comment. A file path covers every
# test in that file; a `file::test` id covers its parametrizations.
#
# THE POINT OF THIS FILE IS TO SHRINK. Fixing an entry means DELETING its line
# (the runner reports a listed entry that now passes as STALE). Deleting the
# TEST instead of fixing it is not a fix — several of these are correct
# assertions reporting real defects, and that is precisely why the lane exists.
#
# Classification:
#   [dep]   missing optional dependency in this venv
#   [env]   needs external state (built target tree, toolchain, live artifact)
#           in a shape this box does not currently have
#   [bug]   genuine defect in shipped code — a correct assertion, unattended
#   [drift] fixture/expectation the code moved past
#
# NOT LISTED HERE, because a manifest entry still RUNS and running this one
# CORRUPTS THE REPO: msvc-src/tools/test_il_fixtures.py::
# TestCrossBundleConsistency::test_stable_json_export re-exports bundle.json in
# place over a tracked fixture. scripts/test_tools.py --deselect's it and prints
# the deselection on every run. See the note in STATIC_ROOTS.
#
# NOT LISTED HERE either, because they were never real: the pre-lane audit
# recorded 45 reds under scripts/unicorn_runner/tests reporting `ImportError:
# cannot import name 'Uc' from 'unicorn'`, and one more in
# msvc-src/tools/test_il_permuter.py. The first 45 were a worktree
# path-resolution bug plus a package-name collision (fixed; see
# scripts/unicorn_runner/unicorn_dep.py) and the last was pollution from the
# fixture-corrupting test above (verified: 6 passed on a clean fixture).

# ── scripts/analysis/tests ────────────────────────────────────────────────────

# RESOLVED 2026-08-17 by lane T1 (merge caf9a1d34) — entry removed, and the
# lane's own STALE check is what said so. The test was not made to pass: its
# INPUT moved to a genuine no-evidence case and it was renamed
# test_fallback_returns_none_when_side_absent, because greening it as written
# would have encoded "mflr implies a frame", which a survey of all 69,307
# functions refutes (306 carry mflr r12 with no allocation). A sibling asserting
# 0 on the OLD input landed alongside it. File is 21 passed.

# ── scripts/orchestrator/tests ────────────────────────────────────────────────

# [drift] agent_runner's env builder no longer emits the keys the tests read:
# KeyError HTTP_PROXY (test_agent_runner.py:112), ALL_PROXY (:121),
# ANTHROPIC_BASE_URL (:151). Either the proxy/auth plumbing moved or it was
# dropped; the tests are the only thing still asserting it exists.
scripts/orchestrator/tests/test_agent_runner.py::TestBuildEnv::test_env_includes_proxy_when_set
scripts/orchestrator/tests/test_agent_runner.py::TestBuildEnv::test_env_includes_socks_proxy_when_set
scripts/orchestrator/tests/test_agent_runner.py::TestBuildAuthEnv::test_openrouter_env_when_enabled

# [drift] `mock.patch` targets `scripts.orchestrator.agent_runner.
# ClaudeAgentOptions`, which the module does not define — the SDK symbol it used
# to import is gone, so all four TestBuildSdkOptions cases die in mock's
# attribute check before reaching an assertion.
scripts/orchestrator/tests/test_agent_runner.py::TestBuildSdkOptions::test_custom_disallowed_tools
scripts/orchestrator/tests/test_agent_runner.py::TestBuildSdkOptions::test_custom_tools_passed_through
scripts/orchestrator/tests/test_agent_runner.py::TestBuildSdkOptions::test_default_tools_when_none
scripts/orchestrator/tests/test_agent_runner.py::TestBuildSdkOptions::test_disallowed_tools_default

# FIXED 2026-09-01 (task #177) — all four entries removed, and the [drift]
# diagnosis they carried was WRONG. It read "context_collector's rendered output
# moved past the tests' string expectations ('Callers (N total):' is no longer
# the xref header)". The writer still emits exactly that header; nothing about
# the rendering had moved.
#
# The real cause: every test in the file patched `DirectGhidraClient`, the
# in-process JVM door, which context_collector only opens under
# GHIDRA_USE_JVM=1 — it was disabled as "broken/hangs" and nothing updated the
# tests. So the mocks arranged nothing, the collector went to the LIVE HTTP
# service on 127.0.0.1:8000, and the assertions graded whether pyghidra-mcp
# happened to be running. Measured both ways on 2026-09-01: with the daemon
# down, test_..._ghidra_unavailable and test_..._xrefs_after_client_error PASS
# and test_xrefs_preview FAILS; with it up, all three flip. Two of these four
# entries were reported STALE for exactly that reason — they were "passing" on
# the daemon being absent, not on anything having been fixed.
#
# Worse, test_xrefs_preview (never manifested) was GREEN when this baseline was
# taken and green VACUOUSLY: the service answered with zero cross-references,
# so the fifteen callers it mocked were never used and it asserted on live data.
#
# The tests now drive production's real cache-first door
# (scripts.orchestrator.database.get_xrefs / get_decompilation) and assert
# inside each test that no live client was constructed. Control: the whole file
# is 33 passed with the daemon simulated UP and answering different data, and
# 33 passed with it down. Sabotage-verified — dropping the 20-line preview
# slice, the header, the callee section, or the cache lookup each turns it red.

# [drift] the refactor prompt no longer contains "./bin/objdiff-cli diff", and
# the pass now runs on haiku where the test still expects sonnet. A model-choice
# change nobody propagated to the test.
scripts/orchestrator/tests/test_refactor_pass.py::TestBuildRefactorPrompt::test_prompt_includes_objdiff_instruction
scripts/orchestrator/tests/test_refactor_pass.py::TestRefactorPassIntegration::test_refactor_runs_when_changes_exist

# ── scripts/unicorn_runner/tests ──────────────────────────────────────────────

# RESOLVED by 57e6e20dd (2026-08-18), entries removed 2026-09-01 (task #177).
# The seven expectations really did describe the pre-Phase-4 classifier; that
# commit updated them to the refined vocabulary (merged_call / merged_arg /
# error / fpr_precision / call_count) and renamed each test to say what it
# checks. Verified: TestClassifyDivergence and TestFormatProbeResult are green.
#
# ⚠ HOW THESE READ BEFORE THEY WERE REMOVED, because it is a manifest failure
# mode worth naming. 57e6e20dd RENAMED six of them
# (test_logic_fpr_mismatch -> test_fpr_mismatch, ...), so the entries matched
# nothing at all and the runner — which infers "not observed failing" ==
# "passes" — printed them under "STALE ... these PASSED". They did not pass;
# they did not exist. Here that was harmless because the renamed tests really
# were fixed in the same commit, but a rename carrying a still-broken test
# would have read identically and deleted live coverage. test_tools.py now
# separates PHANTOM (names no collectable test) from STALE (ran and passed).

# ── tests/ ────────────────────────────────────────────────────────────────────

# FIXED 2026-08-31, and the [bug] classification these two carried was WRONG.
# The entry read "the ingest wrote NULL where it should have written a percent";
# in fact `ingest_report()` has deliberately written no percent since the
# 2026-03-04 verdict-pipeline fix (report.json's fuzzy_match_percent reads 100.0
# for unimplemented stubs, base_size=0, and ingesting it minted thousands of
# false COMPLETE verdicts).  Both tests were [drift] — expectations from before
# that fix — and the same file's `test_unimplemented_functions_are_inserted` had
# been asserting the opposite, current, contract the whole time.  They now assert
# the real contract (no percent, and a re-ingest must not clobber a measured one)
# each with a metadata negative control inside it.

# ── tools/ ────────────────────────────────────────────────────────────────────

# [env] a wall-clock assertion ("Get should be very fast (<10ms)"). Nothing is
# wrong with the cache; the test measures the box. It fails under any concurrent
# load, which a test lane running eight roots is. Should be marked as a
# benchmark or given a much looser bound, not "fixed".
tools/test_cache.py::test_cache_performance

# [env] every BSF-trace test needs the 32-bit debug wibo build driving the real
# MSVC c2.dll under GDB: `RuntimeError: c2.dll never hit wibo PE-mapping
# breakpoint (loader.cpp:452)`. 13 of them, one cause, no toolchain on this box.
# Whole-file entry: the 32 that pass here are the ones that do not need the
# tracer, and they stay green because the manifest matches ids, not files, at
# report time.
#
# SUPERSEDED 2026-08-17 — MOVED TO A DESELECT, not resolved. The 13-failures
# reading was taken in a worktree with NO BUILD, where gdb fails fast (4.2s).
# In the primary checkout, where the build exists, gdb attaches and never
# returns: both this file and test_bsf_pipeline.py measured rc=124 at a 90s
# bound, and the whole root hit its 600s wall-clock. A manifest entry cannot
# hold a hang, because a manifested test still RUNS — and when the root times
# out, every OTHER entry in it goes unjudged. See the deselect block in
# test_tools.py. Still needs the 32-bit debug wibo to revive.
