# YMCA — Full flow from boot to game_screen
#
# Boot screens auto-advance (subsystem stubs). Game data is initialized
# at main_screen via DC3_SCREEN=game_screen env var, which calls
# enter_gameplay to trigger the loading chain directly.
#
# NOTE: Song select → multiuser flow is bypassed because the multiuser
# panel unload crashes (heap corruption in cascade destruction).
# Once that's fixed, this script should navigate through the full
# DTA flow: main → choose_mode → song_select → multiuser → loading.
#
# Usage:
#   DC3_SCREEN=game_screen DC3_SONG=ymcavillagepeople \
#     MILO_HEADLESS=1 MILO_MAX_FRAMES=8000 \
#     MILO_INPUT_SCRIPT=scripts/dc3-input-flows/ymca.txt \
#     DC3_DATA=orig-assets native/build/dc3-native

# Skip attract movie
wait_screen attract_screen
+1 confirm

# Game data init + enter_gameplay fires automatically at main_screen
# (set by DC3_SCREEN=game_screen). Loading chain:
# loading → preloading → real_loading → game_screen
