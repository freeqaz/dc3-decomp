# YMCA — Full flow from boot to game_screen (venue loading)
#
# Route: main_screen → choose_mode (gameplay) → song_select (perform)
#        → scroll to YMCA → select → multiuser → loading → game_screen
#
# The venue loading happens during the loading → game_screen transition
# via FileMerger::FinishLoading merging world assets.
#
# Usage:
#   MILO_HEADLESS=1 MILO_MAX_FRAMES=5000 \
#     MILO_INPUT_SCRIPT=scripts/dc3-input-flows/ymca.txt \
#     DC3_DATA=orig-assets native/build/dc3-native

# main_screen: select "gameplay" (first item)
500 confirm

# choose_mode_screen: select "perform" (first item, already highlighted)
800 confirm

# song_select_screen: scroll down to YMCA
1100 down
1200 down
1300 down

# song_select_screen: select YMCA
1500 confirm

# multiuser_screen: confirm to proceed with defaults
1800 confirm
2000 confirm
