# Permuter Bench Baseline (A0)

Generated by `scripts/permuter/bench/run.py` over the pinned 31-function bench set (`bench_set.json`). Re-run with the same command to get a comparable delta; every later performance PR should post its own row.

- **Run timestamp:** 2026-05-26T23:41:16.068616+00:00
- **git HEAD:** `d6f7b931`
- **functions:** 31  (11 high / 12 mid / 8 low)
- **seed config:** patterns=`all`, max_rounds=3, max_variants=40, plateau_limit=2, compose=True, workers=6, PYTHONHASHSEED=0
- **cache:** fresh/isolated (cold path) — every variant exercises a real compile + objdiff
- **overall wall:** 553.45 s

> Caveat: this baseline was captured while several other permuter sweeps
> were running concurrently on the same host, so the *wall-clock* numbers
> are inflated by CPU contention. The per-call subprocess costs
> (`per_call_ms` below) are the contention-robust signal for the A2/A4
> decision; re-run on a quiet host for a clean wall-clock baseline.

## Headline Metrics

| Metric | Baseline |
|--------|----------|
| variants / second | 1.47 |
| wins / 100 attempts | 12.9  (4/31 functions improved) |
| wall-clock to first 100% | n/a (no function reached 100% on this run) |

Supporting: 816 variants scored across the set; scoring wall 553.45 s; 0 functions reached 100%.

## Per-variant profiling breakdown

Wall-clock attribution across the roadmap's buckets. Two denominators:

- **percent_of_attributed** — share of instrumented work. The honest
  "where does the work go" view (sums to ~100% over real work).
- **percent_of_wall** — share of single-thread wall. With parallel
  compile workers the compile buckets can exceed their wall share
  because N threads accrue time simultaneously.

| Bucket | seconds | % attributed | % wall |
|--------|---------|--------------|--------|
| compile-run | 311.045 | 56.2 | 56.2 |
| python-overhead | 123.8754 | 22.38 | 22.38 |
| objdiff-run | 117.5801 | 21.25 | 21.25 |
| compile-spawn | 0.4926 | 0.09 | 0.09 |
| objdiff-spawn | 0.4537 | 0.08 | 0.08 |

**Measured spawn floors:** compile = 0.815 ms, objdiff = 0.868 ms.  
(The roadmap's unverified "~80 ms objdiff spawn" is replaced by the
measured objdiff spawn floor of 0.868 ms.)

### Per-call subprocess cost (the A2/A4 decision input)

| subprocess | calls | spawn ms | run ms | spawn % of subprocess time |
|------------|-------|----------|--------|----------------------------|
| compile | 521 | 0.946 | 597.015 | 0.16% |
| objdiff | 446 | 1.017 | 263.632 | 0.38% |

## How to reproduce

```bash
# sandbox must be disabled — wibo/cl.exe write objects the sandbox blocks
PYTHONHASHSEED=0 ./venv/bin/python -m scripts.permuter.bench.run --profile \
    --out scripts/permuter/bench/baseline-results.json
```
